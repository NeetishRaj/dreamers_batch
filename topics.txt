JS Part 9: Math, String and Date
################################


Math
    Complete Reference: 
        https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math
    PI
    ceil(), floor(), round()
    max(), min()
    random()
    abs()
    pow()
    sqrt()

String
    Complete  Reference:
        https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String

    Ways of creating String
        single quotes ('hello world')
        double quotes ("hello world")
        back quotes (`hello world`) 

    toLowerCase(), toUpperCase()
    concat()
    includes()
    slice()
    substr()
    trim()
    split()  *


Date
    Complete Reference:
        https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date

    UTC time format  and IST
    Milliseconds elapsed
        Date.now() : Milliseconds elapsed since 1970, Jan 1
    getDate()
    getHours()
    getMinutes()
    getSeconds()


    

Important Notes
###############


Q. Guess the output of this code.

    const arr = [20, -10, 2.5, 60];
    
    console.log(Math.max(...arr)) ?

A. 60


Q. Given the code

    const sentence = "I love to code";

    Write a one-liner code to break this sentence into words array.

A. const words = sentence.split(" "); //['I', 'love', 'to', 'code']



Q. What the difference between the two lines?

    const date1 = Date("2022-08-11");
    const date2 = new Date("2022-08-11");

A. Date("2022-08-11"): we are trying to parse the string into current local time.
    This is the one we use to display local time in websites.
    typeof will be 'string'

    new Date("2022-08-11"): we are parsing date string but creating a date object.
    This is the one we use to do operations on time.
    typeof will be 'object'
